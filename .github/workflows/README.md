# 🚀 CI/CD для Shanks Education

## Автоматическое развертывание на сервер

### Что делает workflow:

1. **📥 Срабатывает** при пуше в `main` ветку
2. **🔄 Обновляет** код на сервере через Git
3. **🔧 Исправляет** права доступа
4. **🔍 Проверяет** конфигурацию nginx
5. **🔄 Перезапускает** веб-сервер
6. **✅ Тестирует** работоспособность сайта

### Настройка секретов

#### Вариант 1: SSH-ключ (Рекомендуемый)

```bash
# 1. Создайте SSH-ключ на локальной машине
ssh-keygen -t rsa -b 4096 -C "github-actions@shanks-education.com"

# 2. Скопируйте приватный ключ
cat ~/.ssh/id_rsa

# 3. Добавьте публичный ключ на сервер
ssh-copy-id root@ВАШ_СЕРВЕР

# 4. Добавьте приватный ключ в GitHub Secrets как SERVER_SSH_KEY
```

#### Вариант 2: Пароль

Просто добавьте пароль сервера в `SERVER_PASS`

### Необходимые секреты GitHub:

| Секрет | Описание | Пример |
|--------|----------|---------|
| `SERVER_HOST` | IP сервера | `155.212.132.62` |
| `SERVER_USER` | Пользователь | `root` |
| `SERVER_SSH_KEY` | Приватный SSH-ключ | `-----BEGIN OPENSSH PRIVATE KEY-----...` |
| `SERVER_PASS` | Пароль (альтернатива) | `ваш_пароль` |
| `SERVER_PORT` | SSH порт | `22` |

### Как добавить секреты:

1. **GitHub** → **Repository** → **Settings** → **Secrets and variables** → **Actions**
2. **New repository secret**
3. Добавьте все необходимые секреты

### Тестирование:

```bash
# Ручной запуск workflow
GitHub → Actions → "Deploy to Server" → "Run workflow"

# Или просто push в main ветку
git add .
git commit -m "Update: добавлена новая функция"
git push origin main
```

### Мониторинг деплоя:

1. **Перейдите** в **Actions** на GitHub
2. **Выберите** последний workflow "Deploy to Server"
3. **Посмотрите логи** выполнения
4. **Проверьте статус** каждого шага

### Логи деплоя включают:

- ✅ Статус обновления кода
- ✅ Проверка конфигурации nginx
- ✅ Статус перезапуска сервисов
- ✅ HTTP-тест сайта
- ✅ Информация о дисковом пространстве

### Устранение проблем:

#### Workflow не запускается:
- Проверьте секреты в GitHub
- Убедитесь, что ветка называется `main` или `master`

#### SSH-соединение падает:
```bash
# Проверьте подключение локально
ssh -i ~/.ssh/id_rsa root@ВАШ_СЕРВЕР

# Проверьте firewall на сервере
sudo ufw status
```

#### Nginx не перезапускается:
```bash
# На сервере проверьте логи
sudo systemctl status nginx
sudo journalctl -u nginx -n 20
```

### Безопасность:

⚠️ **Рекомендации:**
- Используйте SSH-ключи вместо паролей
- Создайте отдельного пользователя для деплоя
- Регулярно обновляйте ключи
- Мониторьте логи доступа

### Структура файлов:

```
.github/
  workflows/
    deploy.yml              # Основной workflow
    deploy-secrets-setup.md # Настройка секретов
    README.md              # Это руководство
```

---

🎉 **Теперь ваш проект автоматически развертывается при каждом пуше!**

При изменении кода просто делайте `git push`, и сайт обновится автоматически! 🚀