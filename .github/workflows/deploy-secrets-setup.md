# 🔐 Настройка секретов для GitHub Actions

## Добавление секретов в GitHub

1. **Перейдите в ваш репозиторий** на GitHub
2. **Settings** → **Secrets and variables** → **Actions**
3. **New repository secret**

## Необходимые секреты:

### `SERVER_HOST`
- **Значение:** IP-адрес вашего сервера
- **Пример:** `155.212.132.62`

### `SERVER_USER`
- **Значение:** Имя пользователя для SSH
- **Пример:** `root`

### `SERVER_SSH_KEY` (Рекомендуемый способ)
- **Значение:** Приватный SSH-ключ
- **Как получить:**
  ```bash
  # На вашем компьютере
  ssh-keygen -t rsa -b 4096 -C "github-actions@yourproject.com"
  cat ~/.ssh/id_rsa
  ```
- **Скопируйте весь вывод** (начиная с `-----BEGIN OPENSSH PRIVATE KEY-----`)

### `SERVER_PORT` (Опционально)
- **Значение:** SSH порт (по умолчанию 22)
- **Пример:** `22`

## Альтернативный способ: SERVER_PASS

Вместо `SERVER_SSH_KEY` можно использовать пароль:

### `SERVER_PASS`
- **Значение:** Пароль от сервера
- **Пример:** `ваш_пароль`

## Настройка SSH-ключа на сервере

Если используете SSH-ключ:

```bash
# На сервере добавьте публичный ключ
echo "ваш_публичный_ключ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

## Проверка workflow

После добавления секретов:
1. **Сделайте push** в ветку `main`
2. **Перейдите** в **Actions** в репозитории
3. **Посмотрите логи** выполнения workflow
4. **Проверьте** результат на сервере

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте приватные ключи в код
- Регулярно обновляйте SSH-ключи
- Используйте отдельного пользователя (не root) для деплоя
- Ограничьте доступ по IP если возможно

## Тестирование

```bash
# Локально протестируйте подключение
ssh -i ~/.ssh/id_rsa user@host "echo 'Connection successful'"

# Проверьте workflow вручную (Actions → Deploy to Server → Run workflow)
```