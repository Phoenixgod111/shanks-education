# 🔑 SSH Setup Guide for GitHub Actions

## 📋 Требуемые секреты

Перейдите: https://github.com/Phoenixgod111/shanks-education/settings/secrets/actions

### Должны быть настроены:

1. **SERVER_HOST** - IP адрес или домен сервера
   - Пример: `123.456.789.0` или `myserver.com`

2. **SERVER_USER** - имя пользователя для SSH
   - Пример: `root` или `ubuntu` или `deploy`

3. **SERVER_SSH_KEY** - приватный SSH ключ
   ```
   -----BEGIN OPENSSH PRIVATE KEY-----
   ... ваш приватный ключ ...
   -----END OPENSSH PRIVATE KEY-----
   ```

4. **SERVER_PASS** - пароль (если нет SSH ключа)
   - Пароль пользователя

5. **SERVER_PORT** - порт SSH (опционально, по умолчанию 22)

## 🧪 Тестирование

### 1. Тест GitHub Actions
Запустите: **🧪 GitHub Actions Test**
- Должен показать: "GitHub Actions is working perfectly!"

### 2. Тест SSH подключения
Запустите: **🔌 SSH Connect Only**
- Должен показать: "SSH CONNECTION ESTABLISHED!"

### 3. Тест деплоя
Запустите: **🚀 Deploy to Server**
- Введите `confirm_deploy: yes`
- Должен выполнить деплой

## 🔧 Генерация SSH ключа

```bash
# На локальной машине
ssh-keygen -t rsa -b 4096 -C "github-actions@your-server.com"

# Скопировать публичный ключ на сервер
ssh-copy-id user@your-server.com

# Или вручную добавить в ~/.ssh/authorized_keys на сервере
```

## 🚨 Возможные проблемы

- **Connection refused** → сервер недоступен или порт закрыт
- **Permission denied** → неправильный SSH ключ или пароль
- **Host key verification failed** → неизвестный SSH хост

## 📞 Поддержка

Если проблемы с SSH, проверьте:
1. SSH доступ с локальной машины: `ssh user@server`
2. Правильность секретов в GitHub
3. Firewall настройки на сервере